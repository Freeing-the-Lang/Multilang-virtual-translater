name: "🌐 Multilang Virtual Translater — Universal AST Prototype (v1.8)"

on:
  push:
    branches: [ main ]
    tags:
      - "v*"
  pull_request:
    types: [ closed ]
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: write

concurrency:
  group: multilang-virtual-translater-${{ github.ref }}
  cancel-in-progress: false  # ✅ 중복 실행 허용

jobs:
  build-test:
    name: "🧠 Build & Test Universal AST"
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    timeout-minutes: 20

    steps:
      - name: "📦 Checkout repository (safe auto-retry)"
        run: |
          set -e
          echo "🔁 Attempting safe git checkout (max 3 retries)..."
          for i in 1 2 3; do
            if git clone --recursive https://github.com/${{ github.repository }} .; then
              echo "✅ Checkout successful."
              break
            else
              echo "⚠️ Retry $i failed. Waiting 10s..."
              sleep 10
            fi
          done
        shell: bash

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          check-latest: true

      - name: "⚙️ Install dependencies (auto-retry)"
        run: |
          set -e
          echo "🔁 Upgrading pip..."
          for i in 1 2 3; do
            python -m pip install --upgrade pip && break || { echo "⚠️ Retry $i failed"; sleep 5; }
          done
          echo "📦 Installing required packages..."
          for i in 1 2 3; do
            pip install tree_sitter pytest && break || { echo "⚠️ Retry $i failed"; sleep 5; }
          done
        shell: bash

      - name: "🧩 Run AST tests"
        run: |
          mkdir -p tests
          echo 'def add(a,b): return a+b' > tests/sample.py
          python - <<'EOF'
          from parsers.parser_python import PythonParser
          from generators.generator_python import PythonGenerator
          code = open("tests/sample.py").read()
          root = PythonParser().parse(code)
          out = PythonGenerator().generate(root)
          print("Generated:\n", out)
          EOF

      - name: "🪵 Archive logs & AST output"
        if: always()
        run: |
          mkdir -p artifacts
          echo "AST Log $(date)" > artifacts/ledger.txt
          cd artifacts
          tar -czf ../multilang_logs_${{ matrix.os }}.tar.gz .
        shell: bash

      - name: "📤 Upload artifacts (OS-specific)"
        uses: actions/upload-artifact@v4
        with:
          name: multilang-virtual-artifacts-${{ matrix.os }}-${{ github.job }}-${{ github.run_id }}
          path: multilang_logs_${{ matrix.os }}.tar.gz

  proof-ledger:
    name: "🧾 ProofLedger Sync"
    runs-on: ubuntu-latest
    needs: build-test
    timeout-minutes: 10

    steps:
      - name: "🧭 Checkout"
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: "📜 Generate ProofLedger"
        run: |
          echo "Multilang Virtual Translater ProofLedger" > PROOFLEDGER.txt
          echo "Timestamp:" $(date) >> PROOFLEDGER.txt
          echo "SHA256 (Ubuntu):" $(sha256sum multilang_logs_ubuntu-latest.tar.gz | cut -d' ' -f1 2>/dev/null || echo "N/A") >> PROOFLEDGER.txt
          echo "SHA256 (macOS):" $(sha256sum multilang_logs_macos-latest.tar.gz | cut -d' ' -f1 2>/dev/null || echo "N/A") >> PROOFLEDGER.txt
          echo "SHA256 (Windows):" $(sha256sum multilang_logs_windows-latest.tar.gz | cut -d' ' -f1 2>/dev/null || echo "N/A") >> PROOFLEDGER.txt
          cat PROOFLEDGER.txt

      - name: "🪣 Commit ProofLedger"
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add PROOFLEDGER.txt
          git commit -m "Update ProofLedger [auto]" || echo "No changes to commit"
          git push || echo "Push skipped (no changes)"

  release:
    name: "🚀 Auto Release (tag-based)"
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build-test, proof-ledger]
    timeout-minutes: 10

    steps:
      - name: "📦 Checkout"
        uses: actions/checkout@v4

      - name: "🧾 Create Release"
        uses: softprops/action-gh-release@v2
        with:
          files: |
            multilang_logs_ubuntu-latest.tar.gz
            multilang_logs_macos-latest.tar.gz
            multilang_logs_windows-latest.tar.gz
            PROOFLEDGER.txt
          body: |
            🔖 **Multilang Virtual Translater — Universal AST Snapshot**
            - Version: ${{ github.ref_name }}
            - Includes OS-specific AST logs & ProofLedger hashes
            - Generated automatically via v1.8 workflow
